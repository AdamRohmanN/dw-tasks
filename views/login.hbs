<!DOCTYPE html>

<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Add Blog</title>
        <link rel="icon" href="assets/logo.png">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <link href="style.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous" defer></script>
        <script src="contact.js" defer></script>
    </head>
    
    <body class="bg-gray">
        <nav class="container-fluid bg-white">
            <div class="container">
                <div class="row d-flex align-items-center py-2">
                    <a class="col-md-1 col-lg-1" href="/"><img src="assets/logo.png" alt="logo"></a>
                    <a class="col-md-2 col-lg-1 btn fw-500" href="/">Home</a>
                    {{#if login}}
                    <a class="col-md-2 col-lg-2 btn fw-500" href="blog-add">Add Project</a>
                    {{else}}
                    <a class="col-md-2 col-lg-2" href="#"></a>
                    {{/if}}
                    <div class="col-md col-lg">{{#if messages.success}}<div class="aler alert-success text-center rounded-pill bold">{{messages.success}}</div>{{/if}}</div>
                    {{#if login}}
                    <a class="col-md col-lg-3 btn fw-500">Hi! {{user.username}}</a>
                    <a class="col-md-2 col-lg-1 btn fw-500" href="/logout">Logout</a>
                    {{else}}
                    <a class="col-md-2 col-lg-1 btn fw-500" href="/register">Register</a>
                    <a class="col-md-2 col-lg-1 btn bold" href="/login">Login</a>
                    {{/if}}
                    <div class="col-md-12 col-lg-2 mt-md-2 mt-lg-0 d-flex">
                        <a class="btn btn-dark flex-fill fw-500" href="contact">Contact Me</a>
                    </div>
                </div>
            </div>
        </nav>

        <section class="container">
            <form class="col-md-8 col-lg-4 d-flex flex-column justify-content-center vh-85 mx-auto" action="/login" method="post">
                <h1 class="text-center bold">Login</h1>
                {{#if messages.danger}}
                <div class="alert alert-danger" role="alert">{{messages.danger}}</div>
                {{/if}}
                <label class="mt-4 fw-500" for="email">Email</label>
                <input id="email" class="input w-100 rounded shadow" name="email" type="email" maxlength="30">
                
                <label class="mt-3 fw-500" for="password">Password</label>
                <input id="password" class="input w-100 rounded shadow" name="password" type="password" maxlength="30">
                
                <div class="d-flex justify-content-end mt-5">
                    <button class="btn btn-dark rounded-pill px-5 bold" type="submit">submit</button>
                </div>
            </form>
        </section>
    </body>
</html>